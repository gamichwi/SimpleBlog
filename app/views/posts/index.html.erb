<h1>Blog Posts</h1>
<% @posts.each do |post| %>
  <div class="card">
    <h3 class="card-title"><%= post.title %></h3>

    <div class="card-body"
    <p><%= post.body %></p>
    <%= link_to "Read More", post_path(post), :class => 'btn btn-primary' %>
    </div>

  </div>
<% end %>